const title = "Dave Shop";
const description = "The Best Shop in internet from Dawid Kaczmarek";

const nextSeoConfig = {
  title,
  description,
  openGraph: {
    title,
    description,

    siteName: "Dave Shop",
  },
};

export default nextSeoConfig;
